# The frontend calls the API at same-origin "/api" by default, which works for
# the single-port production build and for `npm start` (via the CRA proxy).
# Only set this to point at a different API origin.
# REACT_APP_API_URL=http://localhost:5050/api
