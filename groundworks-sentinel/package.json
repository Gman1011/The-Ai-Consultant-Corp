{
  "name": "groundworks-sentinel",
  "version": "1.0.0",
  "private": true,
  "description": "Structural movement monitoring dashboard (Sentinel) with the GARVIS data feed",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "serve": "npm run build && npm run start",
    "dev": "concurrently -k -n API,WEB -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "license": "MIT"
}
